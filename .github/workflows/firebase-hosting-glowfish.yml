# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - glowfish
jobs:
  build_and_deploy_stage:
    runs-on: ubuntu-latest
    env:
      VITE_LINE_CLIENT_ID: ${{ vars.GLOWFISH_STAGE_VITE_LINE_CLIENT_ID }}
      VITE_LINE_CLIENT_SECRET: ${{ secrets.GLOWFISH_STAGE_VITE_LINE_CLIENT_SECRET }}
      VITE_SUPABASE_URL: ${{ vars.GLOWFISH_STAGE_VITE_SUPABASE_URL }}
      VITE_SUPABASE_ANON_KEY: ${{ vars.GLOWFISH_STAGE_VITE_SUPABASE_ANON_KEY }}
      VITE_SERVICE_ROLE_KEY: ${{ secrets.GLOWFISH_STAGE_VITE_SERVICE_ROLE_KEY }}
      VITE_ADMIN_URL: ${{ vars.GLOWFISH_STAGE_VITE_ADMIN_URL }}
      VITE_DEFAULT_STORE: ${{ vars.GLOWFISH_STAGE_VITE_DEFAULT_STORE }}
    steps:
      - uses: actions/checkout@v4
      - run: yarn

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Build
        run: yarn build

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_AI_COMMERCE_C1F4D }}
          channelId: live
          site: glowfish-store-stage
          target: glowfish-store-stage
          projectId: ai-commerce-c1f4d

  build_and_deploy_prod:
    runs-on: ubuntu-latest
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_PROJECT_ID: ${{ vars.GLOWFISH_SUPABASE_PROJECT_ID }}
      VITE_LINE_CLIENT_ID: ${{ vars.GLOWFISH_VITE_LINE_CLIENT_ID }}
      VITE_LINE_CLIENT_SECRET: ${{ secrets.GLOWFISH_VITE_LINE_CLIENT_SECRET }}
      VITE_SUPABASE_URL: ${{ vars.GLOWFISH_VITE_SUPABASE_URL }}
      VITE_SUPABASE_ANON_KEY: ${{ vars.GLOWFISH_VITE_SUPABASE_ANON_KEY }}
      VITE_SERVICE_ROLE_KEY: ${{ secrets.GLOWFISH_VITE_SERVICE_ROLE_KEY }}
      SMS2PRO_API: ${{ secrets.SMS2PRO_API }}
      STORAGE_BUCKET: ${{ vars.STORAGE_BUCKET }}
      VITE_ADMIN_URL: ${{ vars.GLOWFISH_VITE_ADMIN_URL }}
      VITE_DEFAULT_STORE: ${{ vars.GLOWFISH_VITE_DEFAULT_STORE }}
    steps:
      - uses: actions/checkout@v4
      - run: yarn

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: create .env file
        run: |
          echo "VITE_LINE_CLIENT_ID=$VITE_LINE_CLIENT_ID" >> ./supabase/.env
          echo "VITE_LINE_CLIENT_SECRET=$VITE_LINE_CLIENT_SECRET" >> ./supabase/.env
          echo "VITE_SUPABASE_URL=$VITE_SUPABASE_URL" >> ./supabase/.env
          echo "VITE_SUPABASE_ANON_KEY=$VITE_SUPABASE_ANON_KEY" >> ./supabase/.env
          echo "VITE_SERVICE_ROLE_KEY=$VITE_SERVICE_ROLE_KEY" >> ./supabase/.env
          echo "SMS2PRO_API=$SMS2PRO_API" >> ./supabase/.env
          echo "VITE_ADMIN_URL=$VITE_ADMIN_URL" >> ./supabase/.env
          echo "VITE_DEFAULT_STORE=$VITE_DEFAULT_STORE" >> ./supabase/.env
          echo "STORAGE_BUCKET=$STORAGE_BUCKET" >> ./supabase/.env



      - name: update supabase functions and secret
        run: |
          supabase link --project-ref $SUPABASE_PROJECT_ID
          supabase secrets set --env-file ./supabase/.env
          supabase functions deploy

      - name: Build
        run: yarn build

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_AI_COMMERCE_C1F4D }}
          channelId: live
          site: glowfish-store
          target: glowfish-store
          projectId: ai-commerce-c1f4d
